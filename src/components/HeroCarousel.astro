---
/**
 * HeroCarousel Component
 *
 * Carosello hero accessibile con:
 * - ARIA completo (roledescription, tablist, tabpanel)
 * - Pulsante pause/play
 * - Navigazione keyboard
 * - Supporto prefers-reduced-motion
 * - i18n per IT/EN
 */

interface Props {
  lang?: 'it' | 'en';
}

const { lang = 'it' } = Astro.props;

// Slides localizzati
const slides = lang === 'it' ? [
  {
    title: "PAVIMENTI",
    description: "Resine flessibili e resistenti per pavimentazioni continue per l'industria e il privato",
    image: "/images/banner/pavimento.jpg",
    alt: "Pavimentazione industriale in resina con finitura professionale",
    url: "/servizi/pavimenti"
  },
  {
    title: "VERNICIATURA",
    description: "Per la protezione di tutte le superfici, piane e verticali",
    image: "/images/banner/verniciatura.jpg",
    alt: "Verniciatura protettiva di superficie industriale",
    url: "/servizi/verniciatura"
  },
  {
    title: "SABBIATURA",
    description: "Per la pulizia di metalli e altri materiali",
    image: "/images/banner/sabbiatura.jpg",
    alt: "Processo di sabbiatura su superficie metallica",
    url: "/servizi/sabbiatura"
  },
  {
    title: "IMPERMEABILIZZAZIONE",
    description: "Tecnologie innovative con prodotti ad alta resistenza per l'industria e per il privato",
    image: "/images/banner/impermeabilizzazione.jpg",
    alt: "Applicazione di impermeabilizzazione in poliurea",
    url: "/servizi/impermeabilizzazione-poliurea"
  }
] : [
  {
    title: "FLOORING",
    description: "Flexible and resistant resins for continuous flooring for industry and private",
    image: "/images/banner/pavimento.jpg",
    alt: "Industrial resin flooring with professional finish",
    url: "/en/services/flooring"
  },
  {
    title: "PAINTING",
    description: "For the protection of all surfaces, flat and vertical",
    image: "/images/banner/verniciatura.jpg",
    alt: "Protective painting of industrial surface",
    url: "/en/services/painting"
  },
  {
    title: "SANDBLASTING",
    description: "For cleaning metals and other materials",
    image: "/images/banner/sabbiatura.jpg",
    alt: "Sandblasting process on metal surface",
    url: "/en/services/sandblasting"
  },
  {
    title: "WATERPROOFING",
    description: "Innovative technologies with high-resistance products for industry and private",
    image: "/images/banner/impermeabilizzazione.jpg",
    alt: "Polyurea waterproofing application",
    url: "/en/services/waterproofing-polyurea"
  }
];

// Labels localizzati
const labels = {
  carouselLabel: lang === 'it' ? 'Carosello dei servizi principali' : 'Main services carousel',
  prevLabel: lang === 'it' ? 'Slide precedente' : 'Previous slide',
  nextLabel: lang === 'it' ? 'Slide successiva' : 'Next slide',
  pauseLabel: lang === 'it' ? 'Metti in pausa' : 'Pause',
  playLabel: lang === 'it' ? 'Riproduci' : 'Play',
  slideLabel: lang === 'it' ? 'Vai alla slide' : 'Go to slide',
  ofLabel: lang === 'it' ? 'di' : 'of'
};
---

<section
  class="hero-carousel relative w-full overflow-hidden bg-gray-900"
  aria-label={labels.carouselLabel}
  aria-roledescription="carousel"
>
  <div
    class="carousel-container relative w-full h-[calc(100vh-80px)] min-h-[500px]"
    role="group"
    aria-live="off"
    data-autoplay-interval="6000"
  >
    <!-- Slides -->
    {slides.map((item, index) => (
      <div
        class={`carousel-slide absolute inset-0 w-full h-full ${index === 0 ? 'active' : ''}`}
        data-slide={index}
        role="tabpanel"
        id={`slide-${index}`}
        aria-roledescription="slide"
        aria-label={`${index + 1} ${labels.ofLabel} ${slides.length}: ${item.title}`}
        aria-hidden={index !== 0}
        tabindex={index === 0 ? 0 : -1}
      >
        <!-- Image -->
        <img
          src={item.image}
          alt={item.alt}
          class="absolute inset-0 w-full h-full object-cover"
          loading={index === 0 ? "eager" : "lazy"}
          fetchpriority={index === 0 ? "high" : "auto"}
          decoding={index === 0 ? "sync" : "async"}
        />
        
        <!-- Dark Overlay Gradient -->
        <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-black/40 to-black/30 z-10" aria-hidden="true"></div>
        
        <!-- Content -->
        <div class="absolute inset-0 z-20 flex items-center justify-center text-center px-4 md:px-8">
          <div class="max-w-4xl opacity-0 translate-y-8 animate-content">
            <h2 class="text-4xl md:text-6xl lg:text-7xl font-bold text-white mb-6 uppercase tracking-wider text-shadow-lg leading-tight">
              {item.title}
            </h2>
            <p class="text-xl md:text-2xl text-gray-100 mb-8 font-light text-shadow max-w-2xl mx-auto">
              {item.description}
            </p>
            <a
              href={item.url}
              class="inline-flex items-center justify-center px-8 py-4 text-base font-bold text-black uppercase tracking-widest bg-pavesa-yellow hover:bg-white transition-all duration-300 transform hover:-translate-y-1 hover:shadow-lg rounded"
              tabindex={index === 0 ? 0 : -1}
            >
              {lang === 'it' ? 'Scopri di più' : 'Discover more'}
            </a>
          </div>
        </div>
      </div>
    ))}

    <!-- Navigation Dots -->
    <div
      class="absolute bottom-8 left-0 right-0 flex justify-center gap-3 z-30"
      role="tablist"
      aria-label={lang === 'it' ? 'Seleziona slide' : 'Select slide'}
    >
      {slides.map((item, index) => (
        <button
          class={`carousel-dot w-3 h-3 md:w-4 md:h-4 rounded-full border-2 border-white/50 bg-transparent transition-all duration-300 hover:bg-white hover:border-white ${index === 0 ? 'active' : ''}`}
          data-slide={index}
          role="tab"
          aria-selected={index === 0}
          aria-controls={`slide-${index}`}
          aria-label={`${labels.slideLabel} ${index + 1}: ${item.title}`}
        ></button>
      ))}
    </div>

    <!-- Navigation Arrows -->
    <button
      class="carousel-prev hidden md:flex absolute left-8 top-1/2 -translate-y-1/2 z-30 text-white/70 hover:text-pavesa-yellow hover:scale-110 transition-all duration-300 text-6xl leading-none"
      aria-label={labels.prevLabel}
      type="button"
    >
      <i class="fas fa-chevron-left" aria-hidden="true"></i>
    </button>
    <button
      class="carousel-next hidden md:flex absolute right-8 top-1/2 -translate-y-1/2 z-30 text-white/70 hover:text-pavesa-yellow hover:scale-110 transition-all duration-300 text-6xl leading-none"
      aria-label={labels.nextLabel}
      type="button"
    >
      <i class="fas fa-chevron-right" aria-hidden="true"></i>
    </button>

    <!-- Pause/Play Button (Subtle) -->
    <button
      class="carousel-autoplay absolute bottom-8 right-8 z-30 text-white/50 hover:text-white transition-colors"
      aria-label={labels.pauseLabel}
      data-pause-label={labels.pauseLabel}
      data-play-label={labels.playLabel}
      data-playing="true"
      type="button"
    >
      <i class="fas fa-pause text-sm" aria-hidden="true"></i>
    </button>
  </div>
</section>

<style>
  /* Base Slide Styles */
  .carousel-slide {
    opacity: 0;
    z-index: 0;
    transition: opacity 1s ease-in-out;
    pointer-events: none; /* Prevent clicking hidden slides */
  }

  .carousel-slide.active {
    opacity: 1;
    z-index: 10;
    pointer-events: auto;
  }

  /* Content Animation */
  @keyframes contentFadeUp {
    from {
      opacity: 0;
      transform: translateY(2rem);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .carousel-slide.active .animate-content {
    animation: contentFadeUp 0.8s ease-out forwards 0.3s;
  }
  
  /* Reset animation when not active to allow re-trigger */
  .carousel-slide:not(.active) .animate-content {
    opacity: 0;
    transform: translateY(2rem);
    animation: none;
  }

  .carousel-dot.active {
    background-color: var(--pavesa-yellow);
    border-color: var(--pavesa-yellow);
    transform: scale(1.2);
  }

  .text-shadow-lg {
    text-shadow: 0 4px 8px rgba(0, 0, 0, 0.6);
  }

  .text-shadow {
    text-shadow: 0 2px 4px rgba(0, 0, 0, 0.6);
  }

  /* Reduced motion support */
  @media (prefers-reduced-motion: reduce) {
    .carousel-slide {
      transition: none !important;
    }
    .animate-content {
      animation: none !important;
      opacity: 1 !important;
      transform: none !important;
    }
  }
</style>

<script>
  function initCarousel() {
    let currentSlide = 0;
    let autoplayInterval: number | null = null;
    let isPlaying = true;

    const container = document.querySelector('.carousel-container');
    const slides = document.querySelectorAll('.carousel-slide');
    const dots = document.querySelectorAll('.carousel-dot');
    const prevButton = document.querySelector('.carousel-prev');
    const nextButton = document.querySelector('.carousel-next');
    const autoplayButton = document.querySelector('.carousel-autoplay');
    const totalSlides = slides.length;

    if (!container || totalSlides === 0) return;

    // Check for reduced motion preference
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

    function showSlide(index: number, announceChange = true) {
      // Update slides
      slides.forEach((slide, i) => {
        const isActive = i === index;
        slide.classList.toggle('active', isActive);
        slide.setAttribute('aria-hidden', String(!isActive));
        slide.setAttribute('tabindex', isActive ? '0' : '-1');

        // Update link tabindex
        const link = slide.querySelector('a');
        if (link) {
          link.setAttribute('tabindex', isActive ? '0' : '-1');
        }
      });

      // Update dots
      dots.forEach((dot, i) => {
        const isActive = i === index;
        dot.classList.toggle('active', isActive);
        dot.setAttribute('aria-selected', String(isActive));
      });

      currentSlide = index;

      // Announce change to screen readers (only when user-initiated)
      if (announceChange) {
        container.setAttribute('aria-live', 'polite');
        setTimeout(() => {
          container.setAttribute('aria-live', 'off');
        }, 1000);
      }
    }

    function nextSlide(userInitiated = false) {
      const next = (currentSlide + 1) % totalSlides;
      showSlide(next, userInitiated);
    }

    function prevSlide() {
      const prev = (currentSlide - 1 + totalSlides) % totalSlides;
      showSlide(prev, true);
    }

    function startAutoplay() {
      if (prefersReducedMotion) return;

      const interval = parseInt(container.getAttribute('data-autoplay-interval') || '5000');
      autoplayInterval = window.setInterval(() => nextSlide(false), interval);
      isPlaying = true;

      if (autoplayButton) {
        const pauseLabel = autoplayButton.getAttribute('data-pause-label') || 'Pause';
        autoplayButton.setAttribute('aria-label', pauseLabel);
        autoplayButton.setAttribute('data-playing', 'true');
        const icon = autoplayButton.querySelector('i');
        if (icon) {
          icon.classList.remove('fa-play');
          icon.classList.add('fa-pause');
        }
      }
    }

    function stopAutoplay() {
      if (autoplayInterval) {
        clearInterval(autoplayInterval);
        autoplayInterval = null;
      }
      isPlaying = false;

      if (autoplayButton) {
        const playLabel = autoplayButton.getAttribute('data-play-label') || 'Play';
        autoplayButton.setAttribute('aria-label', playLabel);
        autoplayButton.setAttribute('data-playing', 'false');
        const icon = autoplayButton.querySelector('i');
        if (icon) {
          icon.classList.remove('fa-pause');
          icon.classList.add('fa-play');
        }
      }
    }

    function toggleAutoplay() {
      if (isPlaying) {
        stopAutoplay();
      } else {
        startAutoplay();
      }
    }

    // Event listeners for dots
    dots.forEach((dot, index) => {
      dot.addEventListener('click', () => {
        showSlide(index, true);
        stopAutoplay();
        startAutoplay();
      });

      // Keyboard navigation for dots
      dot.addEventListener('keydown', (e: Event) => {
        const keyEvent = e as KeyboardEvent;
        let newIndex: number | null = null;

        switch (keyEvent.key) {
          case 'ArrowLeft':
          case 'ArrowUp':
            newIndex = (index - 1 + totalSlides) % totalSlides;
            break;
          case 'ArrowRight':
          case 'ArrowDown':
            newIndex = (index + 1) % totalSlides;
            break;
          case 'Home':
            newIndex = 0;
            break;
          case 'End':
            newIndex = totalSlides - 1;
            break;
        }

        if (newIndex !== null) {
          keyEvent.preventDefault();
          showSlide(newIndex, true);
          (dots[newIndex] as HTMLElement).focus();
          stopAutoplay();
          startAutoplay();
        }
      });
    });

    // Arrow button listeners
    prevButton?.addEventListener('click', (e) => {
      e.preventDefault();
      prevSlide();
      stopAutoplay();
      startAutoplay();
    });

    nextButton?.addEventListener('click', (e) => {
      e.preventDefault();
      nextSlide(true);
      stopAutoplay();
      startAutoplay();
    });

    // Autoplay toggle
    autoplayButton?.addEventListener('click', toggleAutoplay);

    // Pause on hover (except for reduced motion preference)
    if (!prefersReducedMotion) {
      container.addEventListener('mouseenter', () => {
        if (isPlaying && autoplayInterval) {
          clearInterval(autoplayInterval);
          autoplayInterval = null;
        }
      });

      container.addEventListener('mouseleave', () => {
        if (isPlaying && !autoplayInterval) {
          startAutoplay();
        }
      });
    }

    // Pause on focus within carousel
    container.addEventListener('focusin', () => {
      if (isPlaying && autoplayInterval) {
        clearInterval(autoplayInterval);
        autoplayInterval = null;
      }
    });

    container.addEventListener('focusout', (e: FocusEvent) => {
      // Only restart if focus moves outside the carousel
      if (isPlaying && !container.contains(e.relatedTarget as Node)) {
        startAutoplay();
      }
    });

    // Start autoplay on load (only if reduced motion not preferred)
    if (!prefersReducedMotion) {
      startAutoplay();
    } else {
      // If reduced motion, show pause state
      stopAutoplay();
    }
  }

  // Initialize on page load
  document.addEventListener('DOMContentLoaded', initCarousel);

  // Re-initialize after Astro view transitions
  document.addEventListener('astro:page-load', initCarousel);
</script>
